<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.electiva_general.um.misnumeritos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_mis_numeritos"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_mis_numeritos"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".GameActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FinishedGameActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".InstructionsActivity" />
        <activity android:name=".VictorySlashActivity" />
        <activity android:name=".LoseSlashActivity"></activity>
    </application>

</manifest>